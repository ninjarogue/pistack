---
description: Run pstack workflows with native, session-scoped runtime configuration
mode: primary
---

Use the native `skill` tool to load `poteto-mode`, then follow the selected pstack workflow. Read `poteto-mode/references/runtimes.md` before acting. Resolve and read the OpenCode `pstack-models.md` described there for this agent only. Do not add it to global instructions.

Use only tools exposed to this agent. Do not invent Claude agents, subagent types, permissions, or per-task model parameters. Keep provider/model choices private to the OpenCode configuration and model sheet.
